{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/fancybread-com/agentic-software-development/schemas/command.schema.json",
  "title": "ParsedCommand",
  "description": "Logical structure of a Cursor command markdown file, parsed for validation. Required sections per AGENTS.md §6: Overview, Definitions, Prerequisites, Steps, Tools, Guidance.",
  "type": "object",
  "required": ["overview", "definitions", "prerequisites", "steps", "tools", "guidance"],
  "additionalProperties": false,
  "properties": {
    "overview": {
      "type": "string",
      "minLength": 1,
      "description": "Content of the ## Overview section."
    },
    "definitions": {
      "type": "string",
      "minLength": 1,
      "description": "Content of the ## Definitions section (domain terms, variables like {TASK_KEY})."
    },
    "prerequisites": {
      "type": "string",
      "minLength": 1,
      "description": "Content of the ## Prerequisites section (validation checks before execution)."
    },
    "steps": {
      "type": "object",
      "required": ["content", "numbers"],
      "additionalProperties": false,
      "properties": {
        "content": {
          "type": "string",
          "minLength": 1,
          "description": "Raw content of the ## Steps section."
        },
        "numbers": {
          "type": "array",
          "items": { "type": "integer", "minimum": 1 },
          "minItems": 1,
          "description": "Step numbers extracted from lines matching \\d+\\."
        }
      },
      "description": "Parsed ## Steps: content and at least one numbered step (1., 2., ...)."
    },
    "tools": {
      "type": "string",
      "minLength": 1,
      "description": "Content of the ## Tools section (MCP and other tool documentation)."
    },
    "guidance": {
      "type": "string",
      "minLength": 1,
      "description": "Content of the ## Guidance section (Role, Instruction, Context, Examples, Constraints, Output)."
    },
    "mcpRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^mcp_[A-Za-z0-9-]+_[a-zA-Z0-9_]+$"
      },
      "description": "All MCP tool references (mcp_Server_ToolName) found in the document. Optional; when present, each must match the pattern. mcps/ lookup not yet implemented."
    }
  }
}
